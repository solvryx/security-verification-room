# API Contract

## Problem

Valid JSON can still violate an API response contract.

## Working decision

The demonstration identifies exact paths for type mismatch and missing nested data.

## Negative control

A response matching the same schema returns no issues.

## Claim boundary

Synthetic response data and a small illustrative schema; not a replacement for a full production contract suite.
